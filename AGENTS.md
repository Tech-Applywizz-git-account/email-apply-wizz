# AGENTS.md

## Test commands
- `npx vitest run`
- `npm run lint`
- `npm run build`

## Loop conventions
- Report-only first run until an approved implementation slice begins
- See `LOOP.md` for cadence and human gates
- See `STATE.md` for the current Dashboard Auth slice and gate
